# <执行计划标题>

## 目标

用一段话说明最终想达到的状态。

## 范围

- 包含：
- 不包含：

## 背景

- 相关文档：
- 相关代码路径：
- 已知约束：

## 风险

- 风险：
- 缓解方式：

## 里程碑

1. 调研与方案收敛。
2. 分阶段实现。
3. 验证、交付与收尾。

## 验证方式

- 命令：
- 手工检查：
- 观测检查：

## 进度记录

- [ ] 示例：确认范围和约束。
- [ ] 示例：完成第一个实现切片。
- [ ] 示例：完成验证并记录结果。

## 决策记录

- YYYY-MM-DD：做了什么决定，为什么这么做，会带来什么影响。

## 执行模式

选择一种执行模式（删除不适用的）：

- **交互模式**：人在线，Agent 操作逐步审批。适合架构变更、探索性任务、高风险改动。
- **夜间模式**：人离线，Agent 在 nono 沙箱内 full-access 自主执行。适合边界明确的实施任务。
  - 夜间模式启动前确认：本计划的范围和步骤是否足够清晰？风险是否可控？
  - 启动方式见 `docs/nono-profiles/README.md`。

## 执行文档

执行开始时，在 `docs/exec-runs/<plan-slug>/` 创建以下两份文档（从 `docs/exec-runs/templates/` 复制模板）：

- `execution-process.md`：执行过程中持续更新，记录关键步骤、决定和问题。
- `execution-summary.md`：执行结束后填写，重点写人工验证指引。

夜间模式下，因为没有人工审批环节，这两份文档的完整性尤为重要——它们是人了解执行全貌的唯一途径。
