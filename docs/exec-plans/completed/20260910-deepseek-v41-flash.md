# DeepSeek V4.1 Flash、目录发现与价格更新

状态：实现完成。用户于 2026-09-10 批准实施，2026-09-11 确认提交。

## 范围与决策

- 正式模型 `deepseek:deepseek-flash`，保留旧 Flash 配置和会话读取兼容；按用户 2026-09-11 追加要求移除 Pro；旧 Pro 配置归一到 Flash。
- DeepSeek `/models` 使用现有服务商密钥与 scoped proxy；复用目录缓存服务，以 provider 隔离缓存、查询、添加和刷新。未知 ID 只作发现，不猜测工具或图片能力。
- 本地官方档案提供图片、1M 上下文、384K 输出上限、low/high/max；新 Flash 默认 high。输出上限不等同请求预算。
- 保留官方美元固定高峰估算策略；Flash 2026-09-10 12:00 起使用 0.30 / 1.20 / 0.006 USD（输入/输出/缓存读）。不保留 Pro 定时切换逻辑。仅版本生效日期参与选择，不按峰谷时段切换。历史 Journal 不重算。
- 图片复用 Session artifact 和 Chat Completions；验证格式、大小、user-role、工具回放、直连和代理。
- 不新增依赖，不改其他供应商定价，不提交或推送。

## 执行步骤与验证

1. 更新 shared 模型身份、官方能力/价格事实和设置别名归一；测试旧配置、显式新配置优先、日期边界和端点隔离。
2. 目录服务、网络请求、ModelStore、两套 IPC 与 preload 契约支持 DeepSeek；测试缓存隔离、坏响应、离线、状态保留和未知模型。
3. 设置页刷新和目录添加立即更新已安装模型及 Composer；展示刷新状态、图像能力和高峰价估算；renderer 回归与浅深主题检查。
4. LLM 适配器传递真实模型事实、验证图片请求；回归推理档位、流式输出、工具调用和 usage。
5. 运行 shared/LLM/Desktop 定向测试、依赖构建、desktop typecheck/build、文档与主题门禁；Electron 设置与公开图片最小真实调用。不可完成的真实门禁在摘要说明。
6. 同步模型、成本、设置设计与 history；按学习指南沉淀发现目录和价格快照的边界。

## 依赖和失败处理

官方文档已联网核实。真实 `/models` 和图片调用需要本机已有 DeepSeek API Key；密钥仅由 Main/Host 读取，不输出。网络失败保留最后成功缓存与已安装状态。配置迁移在原有 sanitizer 内幂等完成，保留名称、启用状态和凭据绑定；回滚代码无需修改历史会话。

执行记录：`docs/exec-runs/20260910-deepseek-v41-flash/`。

## 完成与验收边界

模型展示名统一为 `deepseek-flash`，不含脚注编号。自动化回归、构建、类型检查和真实图片请求已有证据。Electron 已验证主界面模型名和 DeepSeek 单模型列表；完整目录刷新交互与浅深主题截图未完成，用户确认转入提交。详见执行摘要。
