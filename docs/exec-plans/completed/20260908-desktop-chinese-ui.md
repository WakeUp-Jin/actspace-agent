# 日常主界面中文统一

状态：2026-09-09 实现与代码验证完成；仓库文档检查通过。

## 目标与授权

2026-09-08 用户批准修订版方案并要求开始执行。统一日常桌面操作文案，保留熟悉的模式和工具执行展示。

## 范围

- 侧栏、输入框普通操作、顶部工作区控件、右侧文件/审查/上下文/终端/子 Agent 列表、现有设置。
- 保留 Chat、Plan、Thinking、Effort 及思考档位；保留工具名称、执行行、执行状态和生成逻辑。
- 保留模型/服务商/协议名称、Skills、Token、命令、路径、用户内容、模型回复和外部错误详情。
- 不涉及轨迹详情、工具执行卡片内部、独立扩展页面、开发诊断、官网、CLI、语言切换或自动翻译。

## 实施约束

- 展示层翻译，不改变 IPC、枚举、配置、Journal 或存储数据。
- 斜杠说明中文化，同时保留命令及原有英文搜索并支持中文搜索。
- 上下文分类按 key 映射；终端默认标题在创建与恢复时一致显示为“终端 N”。
- 标签、Tooltip、无障碍名称同步；只对实际溢出做必要布局修正。
- 现有工作区文档整理改动必须保留，不恢复旧路径，不覆盖其他任务内容。

## 步骤与进度

- [x] 确认源码基线和授权边界。
- [x] 侧栏与输入框中文化。
- [x] 工作区与右侧面板中文化。
- [x] 设置残留补齐、中文搜索与展示回归。
- [x] 工程验证、浏览器/真实 Electron 检查、文档与 history 收尾。

## 验证

- `pnpm --filter @actspace/desktop typecheck`
- `pnpm --filter @actspace/desktop build:renderer`
- 相关 renderer 测试及工具展示回归。
- `pnpm check:docs`
- 浅深主题、窄窗口、主界面/菜单/空态/设置；检查保留英文和工具执行文本未改变。
- 实际观察 Electron 窗口；无法执行的门禁在执行摘要明确记录。

## 风险与回退

主要风险是误改机器字符串、英文搜索退化及中文溢出。使用限定文件和语法节点修改，检查 diff，并通过现有行为测试验证。无需数据迁移，回退本任务差异即可撤销。

## 执行模式与记录

交互模式，用户已批准完整范围。记录见 `docs/exec-runs/20260908-desktop-chinese-ui/`。
