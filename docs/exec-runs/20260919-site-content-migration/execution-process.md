# 官网内容迁移执行过程

## 2026-09-19

- 依据用户确认的 4 篇新增文章和 7 组文档目录实施；来源仓库保留。
- 新增 4 篇原文及 14 张图片，反向还原正文与源文件逐字一致；既有 5 篇的 22 张图片按内容哈希核对。
- 现有评估文章的飞书临时图片返回 HTTP 400；已询问本地原件路径，继续其他内容工作。
- 以当前实现校准设置、只读子任务和工作台文档，不将历史博客作为当前功能承诺。

- 在 Practical 当前工作树与本地所有 Git refs 检查评估文章标题、正文关键词和图片目录；本机导出稿也保留同一失效地址。用户随后提供原始评估架构图，已按原文位置替换为本地资源，其他正文不变。
- 功能文档扩展为 27 页、7 组；新增 22 处截图说明及独立截图清单，保留既有 9 个文档 slug。
- 对照 SettingsContent 纠正多媒体配置入口为通用页，网络搜索入口为工具页；对照最新 preset 写明子 Agent 与 Explore 均为只读。
- 发现欢迎文档同时用于 /docs/ 和自身 slug，相对链接存在层级歧义。增加文档专用构建转换，统一带 base 的路由，博客不处理。
- Astro 7 默认 Sätteri，不再内置 remark 插件处理器；初次 remark 配置构建失败后改用同版本原生 Sätteri 插件，离线添加直接依赖，不改变 Markdown 引擎。
- 最终 pnpm test:site 23/23 通过；pnpm check:site 0 errors/warnings/hints；pnpm build:site 生成 44 页与 37 张正文图片；pnpm check:docs 通过。
- 构建产物遍历 1,526 个本地 href/src，无缺失；9 篇搬运文章的正文摘要一致，37 张图的 SHA-256 一致；27 页中文标点检查通过。
- 浏览器确认评估图正常加载，尺寸 1280×797。预览热更新保留了旧的 Markdown 编译缓存，需要重启开发服务后再次确认欢迎页跳转。

- 开发服务已重启并监听 8780；浏览器读取欢迎页前三个链接，均包含 /actspace-agent/docs/ 正确前缀。Grep/Glob 文章确认原生折叠容器和完整代码入口存在。
