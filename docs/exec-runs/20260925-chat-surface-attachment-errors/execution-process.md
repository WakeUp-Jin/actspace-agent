# Chat 控件与附件错误修复 — 执行过程

- 日期：2026-09-25；交互执行；用户批准上一轮明确方案后实施。
- 保留审批卡与设置页的并行改动，未提交或推送。

## 步骤与验证

1. 核对 Chat 设计边界与现有组件：发现 Composer、窗口顶栏、右栏菜单与空态启动器均有开发入口；按持久化 agentForm 收敛。
2. Chat 隐藏工作区、分支、运行位置、Review、终端和子 Agent；已有开发标签仅隐藏，不删除；停止 Chat 下文件新鲜度探测与终端列表同步。保留工作区归档关系、Context 和可视化回复。
3. 附件校验产生结构化问题，IPC 返回发送前 rejected 分支，preload 不做 projection；App 恢复草稿并收束临时运行状态，Composer 展示中文错误并关联附件 id。
4. 总文本超限携带各附件已解码长度，移除时重算；不静默截断或部分发送。未知异常仍走现有恢复链路，用户只看到可读兜底，技术异常留在日志。
5. 新增独立测试文件以避开另一任务正在修改的应用测试；8 个文件 76 项通过。Desktop typecheck、Renderer/Electron build、主题门禁通过。构建仍有既有大 chunk 警告。
6. Computer Use 初步确认已有 Chat 顶栏、输入框与右栏菜单隐藏正确；正常退出空闲开发应用并用 pnpm dev:log 启动，继续验证新的主进程契约。

## 边界

本轮只修 Chat 控件与附件错误；既有 fork、审批重载恢复与真实大上下文压缩问题仍归原验收任务。
