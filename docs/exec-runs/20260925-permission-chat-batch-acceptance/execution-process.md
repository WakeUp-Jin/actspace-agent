# 权限与 Chat 分组验收 — 执行过程

- 日期：2026-09-25，交互执行；用户批准开始验收，要求合并操作、减少 Computer Use 成本。
- 范围：真实 Electron 权限与 Chat 验收、只读证据核对、定向测试、验收文档。未获代码修复授权，本轮不改产品代码。
- 结果及续验入口：[execution-summary.md](execution-summary.md)。

## 执行记录

1. 阅读仓库协作、架构、前端验收约定及两份既有执行摘要，辨别历史已通过、历史失败和未覆盖项。记录脏工作区基线，保留其他任务改动。
2. 在旧 Agent 会话一次提交 write / edit / protected-read 合成任务。Computer Use 点击精确文件“本会话”授权；Journal 核对写入审批、编辑复用和保护文件拒绝。
3. 创建新 Chat，以真实 PNG 识图和一次五文本附件请求覆盖正向格式。通过原生选择器交互，未用内部 API 代替 UI 上传。
4. 组合负向附件验证；首次隐藏移除按钮点击未生效，读取新 AX 状态发现附件残留。先点击附件使移除入口显现，再移除并读回确认。将 NUL 拆为独立用例，避免把前一个无效附件拦截误当作 NUL 结果。
5. 拒绝非法 UTF-8、超大小和独立 NUL；PDF 被选择器过滤；清理负向附件后成功发送短消息，确认恢复。观察浅色、深色、系统主题，恢复浅色。
6. 运行定向测试共 52 项通过；读取 Chat Journal，核验工具白名单、上下文重放、失败上传不入队。按会话筛选 artifact metadata，核验六份成功制品字节、size 与 SHA-256。
7. 新建隔离 Agent 读取同一合成文件，确认再次请求审批。期间其他任务修改审批 UI，并多次触发 HMR / page reload；审批卡和停止按钮不可见，无法稳定继续卡片交互。询问是否暂停并行修改，未将无回复视为环境已经稳定。
8. 收尾再次读取真实 UI 与 Journal：隔离请求于 19:30:18 自动超时拒绝，19:30:20 turn 完成，没有成功读取或重试。记录该保护通过，但保留审批重载恢复问题。

## 环境污染与限制

- HEAD 从 `9bebd124950f3f139d7e120c8d2c0bb5991a813b` 变化到 `0d4bca17bd7eebacf33b5b1a311131ffe67a0ad4`，同时有正在重写审批卡的未提交文件。验收进程未做任何 Git 写操作。
- 19:13 之后相继出现 ToolLogLine、FileDiffBlock、Delete/Bash、BrowserApprovalBlock 及测试/approval-harness 改动引发的刷新。仅记录相关时间，不把时间相关性作为根因证明。
- 不重复历史已通过的付费图片生成、网页搜索；不制造数十万 token 只为触发压缩。未把这些成本控制决定描述成验收通过。
- 当前目录授权/撤销 UI 缺失；不直接改 backend 状态冒充 Computer Use 验收，也不通过扩大完全权限绕过清理。
- 未全量重启并行开发进程；剩余两组在稳定环境续验，详见摘要。


## 晚间续验

- 20:21 在现有隔离 Agent 发起 read 审批，切换会话后返回卡片正常，主动拒绝成功。
- 20:22 再发起 read，待审批时 Cmd+R；稳定复现卡片和停止按钮消失；20:32 后端自动 timeout deny，未读取文件。
- Chat 上传非法 UTF-8 合成文件，中文提示及草稿保留；键盘移除并重发成功。原生选择器采用 Go To 路径选择，避免把“打开访达项目”误当上传成功。
- 会话级 fork 继承 Chat 与 lineage，子发送再次出现 SESSION_WRITER_LOCKED。仅记录，不越界修复。
- 阈值 50 保存与重新进入读取通过；恢复80，深色观察后恢复浅色。
- 单独发起审批并停止；Journal aborted/denied，UI 短暂标失败。
- Chat 一次批量访问 localhost/loopback/私网，三次全部策略拒绝；retryable=true 分类仍待改。
- 21:09 正常退出后 pnpm dev:log 重启；Chat 恢复和历史附件重放成功，Context 严格两个工具。
- 通过原生 Window 菜单缩窄窗口，检查 Composer、Context 抽屉后恢复原大小。
- 单张真实生成成功，打开右栏目视检查图片；核对 artifact 大小、hash 和归属。
- 48项定向测试通过；产品代码未修改；详细结果、证据序号与剩余矩阵见摘要晚间续验节。
