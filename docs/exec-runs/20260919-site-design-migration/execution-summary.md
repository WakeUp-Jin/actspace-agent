# Site 设计迁移执行摘要

设计入口：[site DESIGN.md](../../../apps/site/DESIGN.md)。

正式 Astro 页面已接入已确认的布局。分类为折叠多选复选框，封面 3:2，详情正文与引言之间保留全宽细线和响应式间距。站点保持浅色，内容继续来自 Markdown 和 release parser。

自动检查：17 个测试通过，Astro check 零错误/警告，静态构建 22 页成功。自动检查不等于 Claude 像素级还原验收。

人工验收：打开首页、文档、博客与更新日志；对照 375px、1137px、1440px 的卡片尺寸与文章分割线。检查分类多选与取消、展开收起、搜索空结果、排序和视图切换；手机导航、目录、复制页面和代码。本站内容与插画不同于参考站，标题换行需要中文场景判断。

不包含线上部署、提交或推送。

## 本轮浏览器验收

正式开发预览已在 8780 启动并返回页面。1137px 视口封面实测 357×238px，与参考相同；多选“上下文工程”和“工具与扩展”显示 4 篇，取消恢复全部；不存在关键词显示 0 篇与空结果提示。375px 列表无横向溢出；详情网格曾因 12 列间距溢出，已改为单列并确认消除，分割线为 1px。

文档检查通过。完整三尺寸逐页视觉验收、剪贴板权限和所有排序/布局组合仍由后续人工验收覆盖。
