# 中间消息区规范

## 定位

中间区域是主工作区，承载对话、工具流和最终回复。

## 消息语法

- 按模型执行顺序展示。
- 一次消息可能包含多轮思考、工具调用、编辑和最终回复。
- 每种消息类型使用不同组件，但保持同一条消息流里的节奏一致。
- 同一轮里的 Thinking、Read/Search、Edit diff 和普通回复使用统一左边缘，不保留旧头像占位缩进。

## 消息类型

- 用户消息。
- 助手普通回复。
- Thinking。
- Read。
- Search。
- Edit diff。
- Final reply。

## 类型规则

- 用户消息用卡片显示。
- 助手普通回复用正常文本块显示，不在消息正文前重复展示头像、产品名或模型名。
- Thinking 默认折叠，点击后展开完整内容。
- Read 和 Search 保持文本流感，不做重边框。
- Edit diff 才使用边框化卡片。
- Final reply 作为收束结果，保持最清晰的阅读层级。

## Thinking 组件

Thinking 是消息流中的折叠思考行。

### 结构

- 一行简短标题，显示思考时长或状态。
- 展开后在下方显示完整思考内容。
- 不使用左侧竖线，不做卡片边框。

### 交互

- 默认折叠。
- 点击后展开完整内容。
- 展开与收起保持同一消息块内的连续性。
- 折叠态使用向右箭头，展开态使用向下箭头。

### 视觉原则

- 保持文本流感。
- 通过排版和间距体现层级，不靠重边框。
- 与普通回复保持统一的主视觉语言。

## Thinking 定稿图

![Thinking 定稿图](image/thinking-final.png)

## Read / Search 组件

Read 和 Search 是和 Thinking 同级别的工具调用消息。

### 结构

- 纯文本行展示。
- 不使用图标。
- 不使用边框卡片。
- 每行只保留工具类型和简短内容。

### 交互

- 默认直接展示，不需要展开。
- 作为消息流中的日志条目存在。
- 与 Thinking 保持同一语法体系，但不从属 Thinking。

### 视觉原则

- 保持最轻量的文本流感。
- 用排版和留白区分层级，不靠装饰。
- 更像执行日志，而不是功能卡片。

## Read / Search 定稿图

![Read / Search 定稿图](image/read-search-final.png)

## Edit diff 组件

Edit diff 是中间消息区唯一使用边框卡片的消息类型。

### 结构

- 顶部一行显示文件图标、文件名和修改统计。
- 主体展示 unified diff 预览。
- 底部用向下标签提示当前是折叠预览态。
- 文件名、统计、diff 预览属于同一个块，不拆成多个卡片。

### 交互

- 默认显示折叠预览。
- 通过向下标签进入完整 diff。
- 与消息流中的其他文本型组件保持顺序一致。

### 视觉原则

- 只在这里使用边框卡片。
- 文件图标可随文件类型变化。
- 修改统计保持轻量但清晰。

## Edit diff 定稿图

![Edit diff 定稿图](image/right-panel-diff-final.png)

## 顺序原则

- 严格按执行顺序展示。
- 工具调用之间不要打乱顺序。
- 同一条消息里的不同状态要保持连续和可读。

## 当前阶段重点

- 先把消息的层级和阅读节奏定清楚。
- 先不追求过多色彩变化。
- 先让组件语法明确，再做细节视觉优化。
