## [2026-06-12 08:50] | Task: 重设计项目 Logo 并替换应用图标与 README wordmark

### 🤖 Execution Context

- **Agent ID**: Cursor Agent
- **Base Model**: Fable 5
- **Runtime**: Cursor IDE (macOS)

### 📥 User Query

> 为项目重新设计 logo（现有版本不够满意），多出几版对比；最终选定「圆角像素阶梯 + 顶点行星」组合方案，应用图标采用白色容器版，并落地替换所有相关资源。

### 🛠 Changes Overview

**Scope:** `packages/desktop`（图标资源）、`docs/assets/readme`（wordmark）、`logo-design/`（新增设计源文件）

**Key Actions:**

- **[设计探索]**: 产出 8 个方向变体 → 用户选定 V1（Pixel Ascent）与 V8（Dual Agents）→ 各 4 个精修变体 → 最终组合为「V1-B Soft 圆角像素 + V1-D 顶点行星」。
- **[设计源文件]**: 全部 SVG 变体与三轮对比展示页（`showcase.html` / `showcase-round2.html` / `final-preview.html`）沉淀在 `logo-design/`。
- **[应用图标]**: 用 `@resvg/resvg-js` 从 `logo-design/svg/appicon-white.svg`（白色圆角容器 + 黑色图形，透明外围）重新生成 `actspace.iconset` 全部 10 个尺寸，并以 `iconutil` 重建 `icon.icns`，同步更新 1024px `icon.png`。
- **[README wordmark]**: 更新 `actspace-agent-wordmark.svg` 中的图形部分为新主标（文字排版保持不变），并重新渲染 PNG。

### 🧠 Design Intent (Why)

新主标的叙事与产品定位对齐：圆角横条是工作台（Space），两个拾级而上的圆角方块是 Agent 的执行步伐（Act），顶端圆形是脱离地面进入轨道的行星——方到圆的渐变暗合「从被动执行到 Kairos 自治运行」。圆角处理让小尺寸（16px）渲染更干净，也更贴近现代桌面应用的图标语言；白色容器版在 macOS Dock 中与系统图标体系一致。

### 📁 Files Modified

- `packages/desktop/resources/icon.icns`
- `packages/desktop/resources/icon.png`
- `packages/desktop/resources/actspace.iconset/*`（10 个尺寸全部重生成）
- `docs/assets/readme/actspace-agent-wordmark.svg`
- `docs/assets/readme/actspace-agent-wordmark.png`
- `logo-design/`（新增：SVG 变体、PNG 预览、三个展示页）
