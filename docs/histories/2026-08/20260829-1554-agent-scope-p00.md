## [2026-08-29 15:54] | Task: 实现 Agent Scope P00 基础契约

### 🤖 Execution Context

- **Agent ID**: `codex`
- **Base Model**: `GPT-5`
- **Runtime**: `Codex desktop`

### 📥 User Query

> 开始实现 Agent Scope 设计规范和执行计划。

### 🛠 Changes Overview

**Scope:** `@actspace/core-scope`

**Key Actions:**

- **opaque identity**：为每个 `AgentScope` 增加实例级 `ScopeKey`，使用 WeakMap 维护 parent relation 并拒绝循环。
- **shared registry**：把 global、exact-scope 和 ancestor chain view 统一到共享 layer storage，支持最近层 shadow 与 parent isolation。
- **quiescent disposal**：增加 disposer 状态和并发 dispose 共享 Promise，确保异步资源静止后才完成清理。
- **contract tests**：覆盖 key identity、rebind、cycle、shadow、child cleanup、disposer race 和失败保留策略。

### 🧠 Design Intent (Why)

Scope identity 必须与 live Agent 实例绑定，不能使用 descriptor/preset/id 字符串；registry visibility 和生命周期所有权必须共享同一 parent relation，避免事件路由、能力查找和清理边界漂移。并发 dispose 必须等待同一个 quiescence boundary，避免 Runtime shutdown 提前返回。

### 📁 Files Modified

- `packages/core/scope/src/scope.ts`
- `packages/core/scope/src/layered-registry.ts`
- `packages/core/scope/src/disposer.ts`
- `packages/core/scope/src/test/scope.test.ts`
- `packages/core/scope/src/test/disposer.test.ts`
- `docs/exec-plans/completed/20260829-actspace-agent-scope-model/README.md`
- `docs/exec-runs/20260829-actspace-agent-scope-model/execution-process.md`
- `docs/exec-runs/20260829-actspace-agent-scope-model/execution-summary.md`

## [2026-08-29 17:05] | Task: 完成 Agent Scope P01/P02 接线

### 🛠 Changes Overview

**Scope:** `@actspace/core-agent`、`@actspace/core-agent-loop`、`@actspace/subagent`、`@actspace/runtime`

**Key Actions:**

- **fused dispatcher**：新增 `createAgentEventDispatcher()`，将 live Agent subject、opaque ScopeKey carrier 与 Cordis dispatch 绑定；通知、串行和 waterfall 共用同一 carrier，subject 错配或 Scope 进入 dispose 后 fail closed。
- **parent-aware admission**：Cordis listener 通过私有 scope tag 注册，事件沿 child → ancestor 准入，兄弟和后代 listener 不会串入；同 descriptor 的并行 live Agent 使用独立 carrier。
- **生产接线**：main Agent、Agent Loop 生命周期通知、one-shot Subagent 都改为实例级 Agent subject；普通 child 默认使用 `parentScope.child()`，显式 `isolatedScope` 才使用 flat `isolatedChild()`。
- **工具边界**：保留现有 Tool definition、executor 和 ToolRuntime 执行逻辑，仅把 Agent event carrier 传入既有工具执行环境。

### ✅ Validation

- `@actspace/core-scope`：18 tests passed。
- `@actspace/core-agent`：11 tests passed。
- `@actspace/core-agent-loop`：4 tests passed。
- `@actspace/subagent`：6 tests passed。
- Cordis adapter：18 tests passed、1 existing skip；Prompt：6 tests passed；Tool Runtime：17 tests passed。
- Runtime typecheck/build、Runtime dependency closure build、文档检查和 `git diff --check` 通过。

真实 Host/Electron/Provider、CLI one-shot 进程 smoke 与全仓库回归仍未宣称完成；对应边界记录在执行摘要中。
