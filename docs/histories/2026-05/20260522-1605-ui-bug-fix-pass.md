# UI bug 修复第一轮

## 用户诉求

用户希望先完整验收当前前端，再按 `bug修复计划` 修复交互 bug 和样式问题，不要边看边随手乱改。

## 本次改动

- 新增并执行 `docs/exec-plans/active/actspace-ui-bug-fix-plan.md` 的第一批修复项。
- 修复右侧面板入口：顶部按钮现在可以切换三栏，并渲染右侧基础 Tab 面板。
- 修复 Composer 浮层状态：模式下拉、模型下拉、Context popup 互斥打开，并支持外部点击和 `Escape` 关闭。
- 修复 Edit diff 展开：默认显示预览，点击底部标签展开完整 diff。
- 调整 mock diff 内容，让展开态在浏览器验收里有明显可见差异。
- 第一轮收敛样式：Composer 默认空态更轻，左侧栏更紧凑，右侧面板基础样式补齐。

## 影响文件

- `packages/desktop/src/renderer/components/WorkbenchLayout.tsx`
- `packages/desktop/src/renderer/components/ConversationView.tsx`
- `packages/desktop/src/renderer/components/Composer.tsx`
- `packages/desktop/src/renderer/components/messages/EditDiffBlock.tsx`
- `packages/desktop/src/renderer/fixtures/workbenchFixture.ts`
- `packages/desktop/src/renderer/styles.css`
- `docs/exec-plans/active/actspace-ui-bug-fix-plan.md`

## 验证

- 已运行 `pnpm --filter @actspace/desktop typecheck`。
- 已用 in-app browser 验证 `http://localhost:5173/`。
- 回归确认右侧面板、下拉互斥、外部关闭、diff 展开、Context popup 均可用。

## 备注

本轮没有做 Electron 真实窗口验收。按照 `docs/FRONTEND_VERIFICATION.md`，桌面壳、preload、IPC 和本地 session 的最终检查留到完整流程末尾。
