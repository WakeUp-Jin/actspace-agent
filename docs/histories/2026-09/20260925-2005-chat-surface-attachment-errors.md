# Chat 开发控件隔离与附件错误恢复

## 用户诉求

修复真实验收发现的 Chat 残留开发控件、附件错误暴露内部 IPC 信息；先给方案，经批准后实施。

## 改动与动机

- Composer、Workbench 和 RightPanel 按 Chat 形态屏蔽开发入口，保留归档关系与对话工具。
- 校验问题结构化返回，主进程不抛可预期的附件异常跨 IPC；preload 和 App 区分发送前拒绝与运行结果，草稿完整恢复。
- 中文提示关联文件，移除附件时清理对应错误，总量超限重算。
- 新增组件、应用、IPC 回归测试；同步 Chat 设计与执行记录。

## 验证

76 项定向测试、Desktop typecheck、Renderer/Electron build、主题门禁通过；真实验收结果见执行摘要。

## 边界与学习

未修改其他任务的审批卡与设置页；未提交或推送。错误作为跨边界值、校验拒绝与执行失败分离，具可迁移性和易错边界，记录到学习文档。
