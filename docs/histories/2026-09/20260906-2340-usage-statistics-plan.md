## 2026-09-06 | Task: 整理使用统计更新执行计划

### Execution Context

- Agent：Codex 当前任务主 Agent。
- Runtime：Codex desktop；先完成计划文档，2026-09-07 获准后实施。

### 用户诉求

> 使用统计参考 Maka 的设计，但视觉与 ActSpace 设置统一；Usage 改为“使用统计”；查清成本未显示的原因，参考 pi 从 models.dev、OpenRouter 生成本地模型目录，避免拉取卡顿，并整理完整执行计划。

### 改动与动机

- 新增页面设计与模型目录/费用设计，区分当前证据、目标契约和未实施状态。
- 拆为四个可独立交付阶段：随包目录与请求费用、统计投影与历史口径、按需后台刷新、页面与最终验收。
- 规定 4 小时 TTL 按需触发，不固定轮询，统计页只读本地投影；目录解析由 worker 承担，失败保留有效缓存。
- 明确 SDK 估算与供应商实际费用的区别、精确模型匹配、原币聚合、未知与免费区分、历史 Journal 不改写。
- 注册执行计划与设计入口，并给历史 Usage 文档追加提案导航。没有改业务代码，没有生成实际模型目录，没有执行实施阶段。

### 关键文件

- `docs/design-docs/frontend/front-usage-statistics-refresh.md`
- `docs/design-docs/model-context/agent-model-catalog-and-usage-cost.md`
- `docs/exec-plans/completed/20260906-actspace-usage-statistics/README.md` 与 P01–P04。

### 验证边界

本轮验证文档链接、计划占位语与仓库文档门禁。费用修复、目录生成、网络刷新、UI 截图、真实 Provider 和 Electron 验收均属于后续实施任务，不能由文档完成推断已通过。

- `pnpm check:docs` 通过，内含 current docs 检查（29 个 truth files）。
- 8 份新增文档、24 个本地链接检查通过；无计划占位语或尾部空格。
- 本轮修改的既有文档 `git diff --check` 通过。

本轮没有代码变更，不单独生成学习文档；实施后按写作指南评估计费来源与目录缓存模式的沉淀价值。

### 2026-09-07 获准实施与交付

用户明确要求开始执行，按 P01–P04 完成模型目录、费用计算 / 来源快照、统计投影、后台更新与中文设置页面。保留既有并行工作区改动，没有提交、推送或真实付费调用。

费用缺失根因为 SDK 零价格占位被误读为真实费用，以及代理引擎未接入费率。现使用实际端点与模型匹配的本地目录估算，保存请求价格依据，历史未知不补零、不按新价自动重算。默认设置宽度、标题与控件替代独立大仪表盘样式。

全仓构建、类型检查及定向测试通过；一万请求冷 / 热投影 23.63 / 3.59ms，worker Node 压测事件循环 p95 16.69ms。浏览器浅 / 深 / 窄窗已观察；真实 Electron 启动退出和真实 Provider / 打包态验收仍未完成。详情见 [执行摘要](../../exec-runs/20260906-actspace-usage-statistics/execution-summary.md)。

满足可迁移、陷阱与模式条目，新增 [零值与费用来源学习笔记](../../learnings/2026-09/20260907-cost-provenance.md)。

## 2026-09-07 Maka 表格对齐与开发历史清理

用户确认保留四项指标，要求下半部分更贴近 Maka 截图，并明确授权删除不需要的开发会话历史。请求日志调整为带分类数量、搜索、状态、详细记录开关、结果数量的八列紧凑表格；模型和工具同表展示，完整名称、Token 分解及费用依据使用可聚焦提示，移除逐行灰色详情块。页面使用设置原语的 880px 宽度选项与 ActSpace 主题。

投影补充会话标题及时间窗分类数量；`tool-calls` 正确归为成功，assistant 缺少用量时读取 step/end，保持每次请求只统计一次。历史费用未知来自旧记录没有可验证的费率依据，并不是必须删除记录才能修复；本轮按用户开发阶段的选择清理。停止开发应用后，删除当前数据根内含 27 份 Journal 的会话目录及其附件，保留配置、连接和模型目录。未在代码中加入启动自动删历史的逻辑。

验证：18 个投影与页面定向测试、Desktop 类型检查、renderer 和 Electron 构建通过。浏览器 fixture 检查浅色桌面、深色 600px 窄窗、分页、详细记录开关和空状态；窄窗文档宽度保持 600px，800px 表格仅在自身容器内滚动。真实 Provider 账单和完整 Electron 交互仍不由 mock 结果代替。

本轮是现有表格交互与投影的局部修正，未新增独立学习文档；费用来源模式已在本任务既有学习文档中记录。

清理后标准开发启动通过：主进程确认 `runtime v2 ready`，当前会话 Journal 为 0，5173 开发服务保持运行。主题颜色、文档门禁与 whitespace 检查通过。

## 2026-09-07 悬浮卡片与固定高峰费用

用户批准移除价目页签，增加模型 / Token 悬浮明细，随后根据实际截图明确要求紧凑化：无标题、单位说明、来源、时间、推理解释或计算式；费用列仅显示金额，不再提供弹窗。最终模型与 Token 卡片宽 208px，只显示名称和数值；Token 保留合计，缓存写入为 0 时隐藏，模型缺少缓存写入价时隐藏该项。旧 activeTab=pricing 回到请求日志。

DeepSeek 官方 V4 模型使用固定高峰 USD 单价，来源和 fixed-peak 策略随请求保存，不按时间或目录更新切换。OpenRouter 官方端点的 Chat Completions / Responses 使用公共 OpenAI SDK 保留原始 usage.cost，真实非负金额（包括 0）优先；其他端点与 SDK 计算值不冒充实际费用。代理仍使用原有 scoped fetch。历史记录不重算，本轮没有清理会话。新增 Radix Hover Card 和明确的 OpenAI SDK 包依赖。

验证：Shared 定价 4 项、费用计算 5 项、代理与 Pi 适配 16 项、真实 OpenAI SDK 配合 stub 流 2 项均通过；最终页面与悬浮组件 7 项通过，活动投影 14 项在前阶段通过。Desktop 类型检查、LLM Pi 类型检查 / 构建、renderer / Electron 构建、主题 / 文档门禁通过。浅色模型卡和深色 Token 卡已用 Computer Use 观察；键盘、Escape、hover、缺价与非零缓存写入由测试覆盖。未发送真实付费请求。

学习沉淀补充到既有费用来源文档：SDK 归一化可能丢失实际金额，展示简化不能削弱持久化依据。

最终启动复查：标准开发命令完成依赖闭包构建，main 日志确认 runtime v2 ready。最终精简浮层通过浏览器浅 / 深主题观察；Computer Use 仍无法按 bundle ID 选择开发应用，因此完整 Electron 交互验收不作通过声明。
