{
  "name": "@actspace/agent-cli",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "main": "dist/cli.js",
  "types": "dist/cli.d.ts",
  "bin": {
    "actspace-agent": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "pnpm run clean && tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@actspace/agent-core": "workspace:*",
    "@actspace/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
