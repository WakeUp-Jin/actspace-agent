<section class="statement site-section">
  <div class="site-page statement-grid">
    <p class="site-kicker">Act + Space</p>
    <div>
      <h2>不是另一个聊天窗口，<br />而是模型的运行空间。</h2>
      <p>
        Agent 的价值不止是生成答案。模型需要持续获得正确上下文，工具调用需要明确权限，
        每一步执行、成本和缓存都应该能够被检查。ActSpace 把这些能力放进同一个本地工作台。
      </p>
    </div>
  </div>
</section>

<style>
  .statement {
    background: var(--site-canvas);
  }

  .statement-grid {
    display: grid;
    grid-template-columns: minmax(160px, 0.6fr) minmax(0, 1.65fr);
    gap: clamp(40px, 8vw, 130px);
  }

  h2 {
    margin-bottom: 32px;
    font-size: clamp(46px, 6vw, 78px);
    font-weight: 520;
    letter-spacing: -0.06em;
    line-height: 1.04;
  }

  .statement-grid > div > p {
    max-width: 760px;
    color: var(--site-body);
    font-size: clamp(18px, 1.8vw, 22px);
    line-height: 1.8;
  }

  @media (max-width: 760px) {
    .statement-grid {
      grid-template-columns: 1fr;
    }

    .statement-grid {
      gap: 28px;
    }
  }
</style>
