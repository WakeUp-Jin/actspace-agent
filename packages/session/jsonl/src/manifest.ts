import { defineBuiltinPluginManifest } from "@actspace/cordis-adapter";
export const manifest = defineBuiltinPluginManifest({ pluginId: "actspace.session.jsonl", version: "0.1.0", name: "ActSpace Session JSONL", entry: { entryId: "session.jsonl", behavior: "./plugin.js" }, host: { required: ["filesystem.session"], optional: [] }, frontend: null, injects: ["session.journal", "session.persistence"], contributions: { services: ["session.jsonl"], tools: [], prompts: [], events: [] } });
