import type { ResolvedComposition } from "./types.js";
export function createBootConfigDump(composition: ResolvedComposition): string { return `${JSON.stringify({ schemaVersion: 1, profileId: composition.profileId, digest: composition.digest, bundles: composition.bundles, entries: composition.entries, services: composition.services ?? [], codecs: composition.codecs ?? [], patches: composition.patchResults, hostCapabilities: composition.hostCapabilities, warnings: composition.warnings, loaderConfig: composition.loaderConfig ?? composition.config, startupRequirements: composition.startupRequirements ?? [] }, null, 2)}\n`; }
