# 官网实拍资源

首页PNG保留必要全景，`docs/`存放文档重点裁剪。原始截图保留在交付归档，不覆盖用户文件。

当前17种画面已接入；新增`docs/file-preview.png`和`docs/tool-approval.png`从同一张用户原图裁剪。模型选择与思考档位共用`docs/model-picker.png`，用量明细共用`docs/usage.png`，正文不重复展示。旧`reasoning-picker.png`保留但不再引用。

待补3张及合并规则见[截图清单](../../../SCREENSHOTS.md)，状态见`src/lib/screenshots.json`。旧命名素材不等同于新版功能证据，不能按文件名直接填补占位。
