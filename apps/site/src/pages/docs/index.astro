---
import DocsLayout from '../../layouts/DocsLayout.astro';
import {getPublicDocs} from '../../lib/docs-navigation';
const docs=await getPublicDocs();const entry=docs.find(d=>d.id==='what-is-actspace')??docs[0];
---
<DocsLayout {entry} welcome/>
