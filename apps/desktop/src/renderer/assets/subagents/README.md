# 子智能体角色头像

来自用户为本次子智能体界面调整提供的九张图片，顺序与附件一致。保留原色、原底或透明通道，逐张裁切留白后缩放到 128 × 128，输出无损 WebP。运行时显示为 32px 或 28px，避免加载完整大图。

`AgentIdentity.tsx` 显式导入全部素材，确保 Vite 打包后 Electron 离线可用。角色颜色仅为媒体内容；状态由独立主题感知图标和文字表达。不要重排素材数组，否则历史子会话的角色映射会变化。
