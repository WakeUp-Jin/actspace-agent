/* Generated by scripts/build-browser-locator-runtime.mjs. Source hash: 99147da636d4f1bb743eb2f18394717d8a42d46f16d9c0d97b4a5e2ef3b49a2f. */
"use strict";
(() => {
  // plugins/browser-bridge/apps/cli/internal/locator/runtime-src/dom.ts
  function normalizeWhitespace(value) {
    return (value ?? "").replace(/\s+/g, " ").trim();
  }
  function textMatches(actual, expected, exact = false) {
    const normalizedActual = normalizeWhitespace(actual);
    const normalizedExpected = normalizeWhitespace(expected);
    if (exact) return normalizedActual === normalizedExpected;
    return normalizedActual.toLocaleLowerCase().includes(normalizedExpected.toLocaleLowerCase());
  }
  function elementBox(element, includeFrameOffsets = true) {
    const rect = element.getBoundingClientRect();
    let x = rect.left;
    let y = rect.top;
    if (includeFrameOffsets) {
      let view = element.ownerDocument.defaultView;
      while (view && view !== view.parent) {
        try {
          const frame = view.frameElement;
          if (!frame) break;
          const frameRect = frame.getBoundingClientRect();
          x += frameRect.left + frame.clientLeft;
          y += frameRect.top + frame.clientTop;
          view = frame.ownerDocument.defaultView;
        } catch {
          break;
        }
      }
    }
    return { x, y, width: rect.width, height: rect.height };
  }
  function isVisible(element) {
    if (!element.isConnected) return false;
    if (element.hasAttribute("hidden") || element.getAttribute("aria-hidden") === "true") return false;
    const style = element.ownerDocument.defaultView?.getComputedStyle(element) ?? getComputedStyle(element);
    if (style.display === "none" || style.visibility === "hidden" || style.visibility === "collapse" || Number(style.opacity) === 0) {
      return false;
    }
    const rect = element.getBoundingClientRect();
    return rect.width > 0 && rect.height > 0;
  }
  function isInViewport(element) {
    if (!isVisible(element)) return false;
    const rect = element.getBoundingClientRect();
    return rect.bottom > 0 && rect.right > 0 && rect.top < window.innerHeight && rect.left < window.innerWidth;
  }
  function isEnabled(element) {
    if (element.getAttribute("aria-disabled") === "true" || element.closest('[aria-disabled="true"]')) return false;
    if ("disabled" in element && Boolean(element.disabled)) return false;
    const disabledFieldset = element.closest("fieldset:disabled");
    if (disabledFieldset && !element.closest("legend")?.isSameNode(disabledFieldset.querySelector("legend"))) return false;
    return true;
  }
  function isEditable(element) {
    if (!isEnabled(element)) return false;
    const tagName = element.tagName.toLowerCase();
    if (tagName === "textarea") return !element.readOnly;
    if (tagName === "input") {
      const input = element;
      return !input.readOnly && !["button", "checkbox", "file", "hidden", "image", "radio", "reset", "submit"].includes(input.type);
    }
    return Boolean(element.isContentEditable);
  }
  function allComposedElements(root) {
    const values = [];
    const visit = (container) => {
      for (const child of Array.from(container.children)) {
        values.push(child);
        if (child.shadowRoot) visit(child.shadowRoot);
        visit(child);
      }
    };
    visit(root);
    return values;
  }
  function queryCSSComposed(root, selector) {
    const matches = [];
    const seen = /* @__PURE__ */ new Set();
    const visit = (container) => {
      for (const element of Array.from(container.querySelectorAll(selector))) {
        if (!seen.has(element)) {
          seen.add(element);
          matches.push(element);
        }
      }
      for (const element of Array.from(container.querySelectorAll("*"))) {
        if (element.shadowRoot) visit(element.shadowRoot);
      }
    };
    try {
      visit(root);
      return matches;
    } catch (error) {
      throw new Error(`unsupported_selector: ${error instanceof Error ? error.message : String(error)}`);
    }
  }
  function composedText(element) {
    const parts = [];
    const visit = (node) => {
      if (node.nodeType === 3) {
        parts.push(node.textContent ?? "");
        return;
      }
      const isElementNode = node.nodeType === 1;
      const isShadowRootNode = node.nodeType === 11 && "host" in node;
      if (!isElementNode && !isShadowRootNode) return;
      const nodeElement = isElementNode ? node : void 0;
      if (nodeElement && (nodeElement.hasAttribute("hidden") || nodeElement.getAttribute("aria-hidden") === "true")) return;
      if (nodeElement?.tagName.toLowerCase() === "slot" && "assignedNodes" in nodeElement) {
        const assigned = nodeElement.assignedNodes({ flatten: true });
        for (const assignedNode of assigned.length > 0 ? assigned : Array.from(node.childNodes)) visit(assignedNode);
        return;
      }
      for (const child of Array.from(node.childNodes)) visit(child);
      if (nodeElement?.shadowRoot) visit(nodeElement.shadowRoot);
    };
    visit(element);
    return normalizeWhitespace(parts.join(" "));
  }
  function isComposedAncestor(ancestor, candidate) {
    let current = candidate;
    while (current) {
      if (current === ancestor) return true;
      const root = current.getRootNode();
      current = current.parentNode ?? (root instanceof ShadowRoot ? root.host : null);
    }
    return false;
  }
  function receivesEvents(element) {
    const ownerDocument = element.ownerDocument;
    if (typeof ownerDocument.elementFromPoint !== "function") return true;
    const rect = element.getBoundingClientRect();
    const x = rect.left + rect.width / 2;
    const y = rect.top + rect.height / 2;
    const hit = ownerDocument.elementFromPoint(x, y);
    return hit === element || isComposedAncestor(element, hit) || isComposedAncestor(hit ?? element, element);
  }
  async function isStable(element) {
    const first = elementBox(element);
    await nextAnimationFrame();
    const second = elementBox(element);
    await nextAnimationFrame();
    const third = elementBox(element);
    return sameBox(first, second) && sameBox(second, third);
  }
  function nextAnimationFrame() {
    return new Promise((resolve) => {
      if (typeof requestAnimationFrame === "function") {
        requestAnimationFrame(() => resolve());
      } else {
        setTimeout(resolve, 16);
      }
    });
  }
  function delay(milliseconds) {
    return new Promise((resolve) => setTimeout(resolve, milliseconds));
  }
  function sameBox(left, right) {
    const epsilon = 0.25;
    return Math.abs(left.x - right.x) <= epsilon && Math.abs(left.y - right.y) <= epsilon && Math.abs(left.width - right.width) <= epsilon && Math.abs(left.height - right.height) <= epsilon;
  }

  // plugins/browser-bridge/apps/cli/internal/locator/runtime-src/accessibility.ts
  var FORM_CONTROL_SELECTOR = "button,input,meter,output,progress,select,textarea";
  function computedRole(element) {
    const explicit = element.getAttribute("role")?.trim().split(/\s+/).find(Boolean);
    if (explicit && explicit !== "none" && explicit !== "presentation") return explicit;
    const tagName = element.tagName.toLowerCase();
    switch (tagName) {
      case "a":
        return element.hasAttribute("href") ? "link" : void 0;
      case "article":
        return "article";
      case "aside":
        return "complementary";
      case "button":
        return "button";
      case "dialog":
        return "dialog";
      case "footer":
        return "contentinfo";
      case "h1":
      case "h2":
      case "h3":
      case "h4":
      case "h5":
      case "h6":
        return "heading";
      case "header":
        return "banner";
      case "img":
        return element.getAttribute("alt") === "" ? void 0 : "img";
      case "li":
        return "listitem";
      case "main":
        return "main";
      case "nav":
        return "navigation";
      case "ol":
      case "ul":
        return "list";
      case "option":
        return "option";
      case "progress":
        return "progressbar";
      case "section":
        return accessibleName(element) ? "region" : void 0;
      case "select": {
        const select = element;
        return select.multiple || select.size > 1 ? "listbox" : "combobox";
      }
      case "summary":
        return "button";
      case "table":
        return "table";
      case "textarea":
        return "textbox";
      case "input":
        return inputRole(element);
      default:
        return void 0;
    }
  }
  function accessibleName(element, visited = /* @__PURE__ */ new Set()) {
    if (visited.has(element)) return "";
    visited.add(element);
    const labelledBy = element.getAttribute("aria-labelledby");
    if (labelledBy) {
      const referenced = labelledBy.split(/\s+/).map((id) => element.ownerDocument.getElementById(id)).filter((value2) => value2 !== null);
      const value = normalizeWhitespace(referenced.map((item) => textAlternative(item, visited, true)).join(" "));
      if (value) return value;
    }
    const ariaLabel = normalizeWhitespace(element.getAttribute("aria-label"));
    if (ariaLabel) return ariaLabel;
    const labels = labelsFor(element);
    if (labels.length > 0) {
      const value = normalizeWhitespace(labels.map((label) => textAlternative(label, visited, false)).join(" "));
      if (value) return value;
    }
    if (element.tagName.toLowerCase() === "img") {
      const alt = normalizeWhitespace(element.alt);
      if (alt) return alt;
    }
    if (element.tagName.toLowerCase() === "input") {
      const input = element;
      if (input.type === "image") return normalizeWhitespace(input.alt || input.title);
      if (["button", "reset", "submit"].includes(input.type)) return normalizeWhitespace(input.value);
    }
    const role = computedRoleWithoutNameRecursion(element);
    if (role && nameFromContent(role)) {
      const value = textAlternative(element, visited, false);
      if (value) return value;
    }
    const title = normalizeWhitespace(element.getAttribute("title"));
    if (title) return title;
    return "";
  }
  function labelsFor(element) {
    if (!element.matches(FORM_CONTROL_SELECTOR)) return [];
    const nativeLabels = "labels" in element ? Array.from(element.labels ?? []) : [];
    if (nativeLabels.length > 0) return nativeLabels;
    const values = [];
    const parent = element.closest("label");
    if (parent?.tagName.toLowerCase() === "label") values.push(parent);
    const id = element.getAttribute("id");
    if (id) {
      for (const label of Array.from(element.ownerDocument.querySelectorAll("label"))) {
        if (label.htmlFor === id && !values.includes(label)) values.push(label);
      }
    }
    return values;
  }
  function textAlternative(element, visited, referenced) {
    if (!referenced && (element.hasAttribute("hidden") || element.getAttribute("aria-hidden") === "true")) return "";
    const nestedLabel = normalizeWhitespace(element.getAttribute("aria-label"));
    if (nestedLabel) return nestedLabel;
    if (element.tagName.toLowerCase() === "img") return normalizeWhitespace(element.alt);
    const direct = composedText(element);
    if (direct) return direct;
    return accessibleName(element, visited);
  }
  function inputRole(input) {
    switch (input.type) {
      case "button":
      case "image":
      case "reset":
      case "submit":
        return "button";
      case "checkbox":
        return "checkbox";
      case "number":
        return "spinbutton";
      case "radio":
        return "radio";
      case "range":
        return "slider";
      case "search":
        return "searchbox";
      case "email":
      case "tel":
      case "text":
      case "url":
        return input.hasAttribute("list") ? "combobox" : "textbox";
      default:
        return void 0;
    }
  }
  function computedRoleWithoutNameRecursion(element) {
    if (element.tagName.toLowerCase() === "section") return void 0;
    return computedRole(element);
  }
  function nameFromContent(role) {
    return (/* @__PURE__ */ new Set([
      "button",
      "cell",
      "checkbox",
      "columnheader",
      "gridcell",
      "heading",
      "link",
      "listitem",
      "menuitem",
      "menuitemcheckbox",
      "menuitemradio",
      "option",
      "radio",
      "row",
      "rowheader",
      "switch",
      "tab",
      "tooltip",
      "treeitem"
    ])).has(role);
  }

  // plugins/browser-bridge/apps/cli/internal/locator/runtime-src/locator.ts
  var DEFAULT_TIMEOUT_MS = 1e4;
  function normalizeTarget(params) {
    if (params.target) return normalizeStructuredTarget(params.target);
    if (params.selector) return { kind: "css", value: params.selector };
    throw new Error("locator_target_required: provide target or selector");
  }
  function resolveAll(target) {
    const { root, leafTarget } = resolveFramePath(target);
    return resolveWithin(root, leafTarget);
  }
  async function waitForStrict(target, options = {}) {
    const timeoutMs = normalizedTimeout(options.timeoutMs);
    const deadline = Date.now() + timeoutMs;
    let lastReason = "selector_not_found";
    let lastCount = 0;
    while (Date.now() <= deadline) {
      const matches = resolveAll(target);
      lastCount = matches.length;
      const strict = strictCandidate(matches);
      if (!strict.element) {
        lastReason = strict.reason;
        await delay(50);
        continue;
      }
      if (!options.actionable && !options.editable) return strict.element;
      strict.element.scrollIntoView({ block: "center", inline: "nearest" });
      const state = await actionability(strict.element);
      const acceptable = state.visible && state.enabled && state.stable && state.receivesEvents && (!options.editable || state.editable);
      if (acceptable) return strict.element;
      lastReason = state.reason ?? "element_not_actionable";
      await delay(50);
    }
    throw new Error(`locator_timeout: ${describeTarget(target)}; matches=${lastCount}; reason=${lastReason}`);
  }
  async function waitForState(target, state, timeoutMs) {
    const deadline = Date.now() + normalizedTimeout(timeoutMs);
    let lastCount = 0;
    while (Date.now() <= deadline) {
      const matches = resolveAll(target);
      lastCount = matches.length;
      const hasVisible = matches.some(isVisible);
      if (state === "attached" && matches.length > 0) return;
      if (state === "detached" && matches.length === 0) return;
      if (state === "visible" && hasVisible) return;
      if (state === "hidden" && (matches.length === 0 || !hasVisible)) return;
      await delay(50);
    }
    throw new Error(`locator_timeout: ${describeTarget(target)} did not become ${state}; matches=${lastCount}`);
  }
  async function actionPoint(target, timeoutMs, localCoordinates = false) {
    const element = await waitForStrict(target, { timeoutMs, actionable: true });
    element.scrollIntoView({ block: "center", inline: "nearest" });
    const state = await actionability(element);
    if (!state.visible) throw new Error("element_not_visible");
    if (!state.enabled) throw new Error("element_disabled");
    if (!state.receivesEvents) throw new Error("element_intercepted");
    if (!state.stable) throw new Error("element_not_stable");
    const box = elementBox(element, !localCoordinates);
    return { x: box.x + box.width / 2, y: box.y + box.height / 2, box };
  }
  async function actionability(element) {
    const visible = isVisible(element);
    if (!visible) return { visible, enabled: isEnabled(element), editable: isEditable(element), stable: false, receivesEvents: false, reason: "element_not_visible" };
    const enabled = isEnabled(element);
    if (!enabled) return { visible, enabled, editable: false, stable: false, receivesEvents: false, reason: "element_disabled" };
    const stable = await isStable(element);
    if (!stable) return { visible, enabled, editable: isEditable(element), stable, receivesEvents: false, reason: "element_not_stable" };
    const canReceiveEvents = receivesEvents(element);
    return {
      visible,
      enabled,
      editable: isEditable(element),
      stable,
      receivesEvents: canReceiveEvents,
      ...canReceiveEvents ? {} : { reason: "element_intercepted" }
    };
  }
  function describeTarget(target) {
    const framePrefix = target.framePath?.length ? `frames=${target.framePath.length} ` : "";
    switch (target.kind) {
      case "role":
        return `${framePrefix}role=${target.role ?? target.value ?? ""} name=${target.name ?? ""}`.trim();
      case "text":
        return `${framePrefix}text=${target.value ?? target.name ?? ""}`;
      case "label":
        return `${framePrefix}label=${target.value ?? target.name ?? ""}`;
      case "placeholder":
        return `${framePrefix}placeholder=${target.value ?? target.name ?? ""}`;
      case "test_id":
        return `${framePrefix}test_id=${target.value ?? ""}`;
      case "css":
        return `${framePrefix}css=${target.value ?? ""}`;
    }
  }
  function resolveFramePath(target) {
    let root = document;
    for (const rawFrameTarget of target.framePath ?? []) {
      const frameTarget = normalizeStructuredTarget({ ...rawFrameTarget, framePath: void 0 });
      const frameMatches = resolveWithin(root, frameTarget);
      const frame = strictCandidate(frameMatches).element;
      if (!(frame instanceof HTMLIFrameElement)) {
        throw new Error(`frame_not_found: ${describeTarget(frameTarget)}`);
      }
      let frameDocument = null;
      try {
        frameDocument = frame.contentDocument;
      } catch {
        frameDocument = null;
      }
      if (!frameDocument) throw new Error(`frame_cross_origin: ${describeTarget(frameTarget)}`);
      root = frameDocument;
    }
    return { root, leafTarget: { ...target, framePath: void 0 } };
  }
  function resolveWithin(root, target) {
    switch (target.kind) {
      case "css": {
        const selector = requiredValue(target, "css");
        return queryCSSComposed(root, selector);
      }
      case "role": {
        const role = target.role ?? target.value;
        if (!role) throw new Error("invalid_locator_target: role target requires role");
        return allComposedElements(root).filter((element) => {
          if (computedRole(element) !== role) return false;
          if (target.name === void 0) return true;
          return textMatches(accessibleName(element), target.name, target.exact);
        });
      }
      case "text": {
        const expected = target.value ?? target.name;
        if (!expected) throw new Error("invalid_locator_target: text target requires value");
        const matches = allComposedElements(root).filter((element) => textMatches(composedText(element), expected, target.exact));
        return matches.filter((element) => !Array.from(element.children).some((child) => matches.includes(child)));
      }
      case "label": {
        const expected = target.value ?? target.name;
        if (!expected) throw new Error("invalid_locator_target: label target requires value");
        return allComposedElements(root).filter((element) => labelsFor(element).some((label) => textMatches(composedText(label), expected, target.exact)));
      }
      case "placeholder": {
        const expected = target.value ?? target.name;
        if (!expected) throw new Error("invalid_locator_target: placeholder target requires value");
        return allComposedElements(root).filter((element) => textMatches(element.getAttribute("placeholder") ?? "", expected, target.exact));
      }
      case "test_id": {
        const expected = requiredValue(target, "test_id");
        return allComposedElements(root).filter((element) => element.getAttribute("data-testid") === expected);
      }
    }
  }
  function strictCandidate(matches) {
    if (matches.length === 1) return { element: matches[0], reason: "ok" };
    const visible = matches.filter(isVisible);
    if (visible.length === 1) return { element: visible[0], reason: "ok" };
    if (matches.length === 0) return { reason: "selector_not_found" };
    return { reason: `selector_ambiguous:${matches.length}` };
  }
  function normalizeStructuredTarget(target) {
    const rawKind = String(target.kind ?? "").toLowerCase();
    const kind = rawKind === "testid" || rawKind === "test-id" ? "test_id" : rawKind;
    if (!["css", "role", "text", "label", "placeholder", "test_id"].includes(kind)) {
      throw new Error(`invalid_locator_target: unsupported kind ${target.kind}`);
    }
    return { ...target, kind };
  }
  function requiredValue(target, kind) {
    if (!target.value) throw new Error(`invalid_locator_target: ${kind} target requires value`);
    return target.value;
  }
  function normalizedTimeout(timeoutMs) {
    const value = Number(timeoutMs ?? DEFAULT_TIMEOUT_MS);
    if (!Number.isFinite(value) || value <= 0) return DEFAULT_TIMEOUT_MS;
    return Math.min(6e4, Math.max(1, Math.trunc(value)));
  }

  // plugins/browser-bridge/apps/cli/internal/locator/runtime-src/runtime.ts
  var INTERACTABLE_SELECTOR = "a,button,input,textarea,select,summary,details,label,img,video,audio,[role],[onclick],[contenteditable=true],[tabindex],[draggable=true]";
  var SNAPSHOT_LIMIT_MAX = 1e3;
  var PAGE_LIMIT_DEFAULT = 200;
  var PAGE_LIMIT_MAX = 1e3;
  function createRuntime(version, buildHash) {
    let snapshotGeneration = 0;
    let snapshotNodes = /* @__PURE__ */ new Map();
    const invoke = async (action, params = {}) => {
      switch (action) {
        case "point":
          return actionPoint(normalizeTarget(params), params.timeoutMs, params.localCoordinates);
        case "frame_element":
          return waitForStrict(normalizeTarget(params), { timeoutMs: params.timeoutMs });
        case "frame_offset": {
          const frame = await waitForStrict(normalizeTarget(params), { timeoutMs: params.timeoutMs, actionable: true });
          if (frame.tagName.toLowerCase() !== "iframe") throw new Error("frame_not_found");
          const rect = frame.getBoundingClientRect();
          return {
            x: rect.left + frame.clientLeft,
            y: rect.top + frame.clientTop
          };
        }
        case "fill": {
          const target = normalizeTarget(params);
          const element = await waitForStrict(target, { timeoutMs: params.timeoutMs, actionable: true, editable: true });
          setNativeValue(element, params.value ?? "");
          return {};
        }
        case "focus": {
          const element = await waitForStrict(normalizeTarget(params), { timeoutMs: params.timeoutMs });
          if (!("focus" in element) || typeof element.focus !== "function") throw new Error("element_not_focusable");
          element.focus();
          return {};
        }
        case "select_option": {
          const element = await waitForStrict(normalizeTarget(params), { timeoutMs: params.timeoutMs, actionable: true });
          if (element.tagName.toLowerCase() !== "select") throw new Error("element_not_select");
          const select = element;
          const selections = params.selections ?? [];
          const selected = [];
          for (const option of Array.from(select.options)) {
            const match = selections.some((selection) => selection.value === option.value || selection.label === option.label || selection.valueOrLabel === option.value || selection.valueOrLabel === option.label);
            option.selected = match;
            if (match) selected.push(option.value);
          }
          dispatchFormEvent(select, "input");
          dispatchFormEvent(select, "change");
          return { values: selected };
        }
        case "set_checked": {
          const target = normalizeTarget(params);
          const element = await waitForStrict(target, { timeoutMs: params.timeoutMs, actionable: true });
          if (element.tagName.toLowerCase() !== "input") throw new Error("element_not_checkable");
          const input = element;
          if (!["checkbox", "radio"].includes(input.type)) throw new Error("element_not_checkable");
          if (input.type === "radio" && params.checked === false) throw new Error("radio_cannot_uncheck");
          return { value: input.checked, point: await actionPoint(target, params.timeoutMs, params.localCoordinates) };
        }
        case "checked_state": {
          const element = await waitForStrict(normalizeTarget(params), { timeoutMs: params.timeoutMs });
          if (element.tagName.toLowerCase() !== "input") throw new Error("element_not_checkable");
          const input = element;
          if (!["checkbox", "radio"].includes(input.type)) throw new Error("element_not_checkable");
          return { value: input.checked };
        }
        case "inner_text": {
          const element = await waitForStrict(normalizeTarget(params), { timeoutMs: params.timeoutMs });
          return { value: "innerText" in element ? element.innerText : element.textContent };
        }
        case "text_content": {
          const element = await waitForStrict(normalizeTarget(params), { timeoutMs: params.timeoutMs });
          return { value: element.textContent };
        }
        case "all_text_contents": {
          const target = normalizeTarget(params);
          await waitForState(target, "attached", params.timeoutMs);
          return paginate(resolveAll(target).map((element) => element.textContent ?? ""), params);
        }
        case "read_all": {
          const target = normalizeTarget(params);
          await waitForState(target, "attached", params.timeoutMs);
          const values = resolveAll(target).flatMap((element) => {
            const relative = params.relativeSelector ? Array.from(element.querySelectorAll(params.relativeSelector)) : [element];
            return relative.map((candidate) => ({
              attributes: Object.fromEntries(Array.from(candidate.attributes).map((attribute) => [attribute.name, attribute.value])),
              inner_text: "innerText" in candidate ? candidate.innerText ?? "" : "",
              text_content: candidate.textContent ?? "",
              role: computedRole(candidate),
              accessible_name: accessibleName(candidate) || void 0
            }));
          });
          return paginate(values, params);
        }
        case "get_attribute": {
          const element = await waitForStrict(normalizeTarget(params), { timeoutMs: params.timeoutMs });
          return { value: element.getAttribute(params.name ?? "") };
        }
        case "is_visible":
          return { value: resolveAll(normalizeTarget(params)).some(isVisible) };
        case "is_enabled": {
          const element = await waitForStrict(normalizeTarget(params), { timeoutMs: params.timeoutMs });
          return { value: isEnabled(element) };
        }
        case "count":
          return { count: resolveAll(normalizeTarget(params)).length };
        case "wait_for": {
          await waitForState(normalizeTarget(params), params.state ?? "visible", params.timeoutMs);
          return {};
        }
        case "dom_snapshot":
          return { dom_snapshot: document.body?.innerText || document.body?.textContent || "" };
        case "element_info":
          return { elements: elementsAtPoint(params).map(describe) };
        case "visible_dom": {
          snapshotGeneration += 1;
          snapshotNodes = /* @__PURE__ */ new Map();
          const limit = boundedInteger(params.limit, 500, SNAPSHOT_LIMIT_MAX);
          const elements = allComposedElements(document).filter((element) => element.matches(INTERACTABLE_SELECTOR)).filter(isInViewport);
          const nodes = elements.slice(0, limit).map((element, index) => {
            const nodeId = `${snapshotGeneration}:${index + 1}`;
            snapshotNodes.set(nodeId, element);
            return { nodeId, ...describe(element) };
          });
          return {
            generation: snapshotGeneration,
            total: elements.length,
            returned: nodes.length,
            truncated: nodes.length < elements.length,
            nodes
          };
        }
        case "node_point":
          return pointForElement(snapshotNode(snapshotNodes, params.nodeId));
        case "node_scroll": {
          const element = snapshotNode(snapshotNodes, params.nodeId);
          if ("scrollBy" in element && typeof element.scrollBy === "function") {
            element.scrollBy(params.scrollX ?? 0, params.scrollY ?? 0);
          } else {
            element.scrollIntoView({ block: "center", inline: "nearest" });
          }
          return {};
        }
        case "download_media_at_point":
          return triggerDownload(document.elementFromPoint(params.x ?? 0, params.y ?? 0));
        case "download_media_selector":
          return triggerDownload(await waitForStrict(normalizeTarget(params), { timeoutMs: params.timeoutMs }));
        case "node_download_media":
          return triggerDownload(snapshotNode(snapshotNodes, params.nodeId));
        case "clipboard_read_text":
          return { text: await navigator.clipboard.readText() };
        case "clipboard_write_text":
          await navigator.clipboard.writeText(params.text ?? "");
          return {};
        case "clipboard_read":
          return readClipboard();
        case "clipboard_write":
          await writeClipboard(params.items ?? []);
          return {};
        default:
          throw new Error(`invalid_locator_action: ${action}`);
      }
    };
    return { version, buildHash, invoke, isVisible, isEnabled, isEditable };
  }
  function setNativeValue(element, value) {
    const tagName = element.tagName.toLowerCase();
    const view = element.ownerDocument.defaultView;
    if (tagName === "input") {
      Object.getOwnPropertyDescriptor(view?.HTMLInputElement.prototype ?? HTMLInputElement.prototype, "value")?.set?.call(element, value);
    } else if (tagName === "textarea") {
      Object.getOwnPropertyDescriptor(view?.HTMLTextAreaElement.prototype ?? HTMLTextAreaElement.prototype, "value")?.set?.call(element, value);
    } else if (Boolean(element.isContentEditable)) {
      element.textContent = value;
    } else {
      throw new Error("element_not_editable");
    }
    dispatchFormEvent(element, "input", value);
    dispatchFormEvent(element, "change");
  }
  function dispatchFormEvent(element, type, value) {
    const view = element.ownerDocument.defaultView;
    if (type === "input" && typeof view?.InputEvent === "function") {
      element.dispatchEvent(new view.InputEvent(type, { bubbles: true, inputType: "insertText", data: value ?? null }));
      return;
    }
    const EventConstructor = view?.Event ?? Event;
    element.dispatchEvent(new EventConstructor(type, { bubbles: true }));
  }
  function pointForElement(element) {
    element.scrollIntoView({ block: "center", inline: "nearest" });
    const box = elementBox(element);
    return { x: box.x + box.width / 2, y: box.y + box.height / 2, box };
  }
  function describe(element) {
    const rawText = ("innerText" in element ? element.innerText : element.textContent) || element.textContent || "";
    const text = rawText.trim().replace(/\s+/g, " ");
    return {
      tagName: element.tagName.toLowerCase(),
      role: computedRole(element),
      text: text.slice(0, 500),
      textTruncated: text.length > 500 || void 0,
      originalTextChars: text.length > 500 ? text.length : void 0,
      ariaName: accessibleName(element) || void 0,
      href: element.tagName.toLowerCase() === "a" ? element.getAttribute("href") || void 0 : void 0,
      type: element.getAttribute("type") || void 0,
      visible: isVisible(element),
      enabled: isEnabled(element),
      editable: isEditable(element),
      checked: "checked" in element ? Boolean(element.checked) : void 0,
      boundingBox: elementBox(element)
    };
  }
  function elementsAtPoint(params) {
    const x = params.x ?? 0;
    const y = params.y ?? 0;
    const values = typeof document.elementsFromPoint === "function" ? document.elementsFromPoint(x, y) : document.elementFromPoint(x, y) ? [document.elementFromPoint(x, y)] : [];
    return values.slice(0, 10).filter((element) => params.includeNonInteractable || isVisible(element));
  }
  function paginate(values, params) {
    const total = values.length;
    const offset = Math.min(total, Math.max(0, Math.trunc(Number(params.offset) || 0)));
    const limit = boundedInteger(params.limit, PAGE_LIMIT_DEFAULT, PAGE_LIMIT_MAX);
    const page = values.slice(offset, offset + limit);
    return { values: page, total, offset, returned: page.length, has_more: offset + page.length < total };
  }
  function boundedInteger(value, fallback, maximum) {
    return Math.min(maximum, Math.max(1, Math.trunc(Number(value) || fallback)));
  }
  function snapshotNode(nodes, nodeId) {
    const element = nodeId ? nodes.get(nodeId) : void 0;
    if (!element || !element.isConnected) throw new Error(`node_snapshot_stale: ${nodeId ?? ""}`);
    return element;
  }
  function triggerDownload(element) {
    if (!element) throw new Error("download_media_not_found");
    const media = element.closest("a[href],img,video,audio,source") ?? element;
    const candidate = media;
    const url = candidate.href || candidate.currentSrc || candidate.src;
    if (!url) throw new Error("download_media_not_found");
    const ownerDocument = media.ownerDocument;
    const anchor = ownerDocument.createElement("a");
    anchor.href = url;
    anchor.download = media.tagName.toLowerCase() === "a" ? candidate.download : "";
    anchor.style.display = "none";
    ownerDocument.body.appendChild(anchor);
    anchor.click();
    anchor.remove();
    return { url };
  }
  async function readClipboard() {
    const clipboardItems = await navigator.clipboard.read();
    const items = [];
    for (const clipboardItem of clipboardItems.slice(0, 8)) {
      const entries = [];
      for (const type of clipboardItem.types.slice(0, 8)) {
        const blob = await clipboardItem.getType(type);
        if (blob.size > 1024 * 1024) throw new Error("clipboard_item_too_large");
        if (type.startsWith("text/")) {
          entries.push({ mime_type: type, text: await blob.text() });
          continue;
        }
        const bytes = new Uint8Array(await blob.arrayBuffer());
        let binary = "";
        for (let index = 0; index < bytes.length; index += 32768) {
          binary += String.fromCharCode(...bytes.subarray(index, index + 32768));
        }
        entries.push({ mime_type: type, base64: btoa(binary) });
      }
      items.push({ entries, presentation_style: clipboardItem.presentationStyle || "unspecified" });
    }
    return { items };
  }
  async function writeClipboard(items) {
    const clipboardItems = items.map((item) => {
      const values = {};
      for (const entry of item.entries ?? []) {
        const mimeType = entry.mimeType || entry.mime_type;
        if (!mimeType) throw new Error("clipboard_mime_type_required");
        if (typeof entry.text === "string") {
          values[mimeType] = new Blob([entry.text], { type: mimeType });
          continue;
        }
        const binary = atob(entry.base64 || "");
        if (binary.length > 1024 * 1024) throw new Error("clipboard_item_too_large");
        values[mimeType] = new Blob([Uint8Array.from(binary, (character) => character.charCodeAt(0))], { type: mimeType });
      }
      const rawPresentationStyle = item.presentationStyle || item.presentation_style;
      const presentationStyle = rawPresentationStyle === "inline" || rawPresentationStyle === "attachment" ? rawPresentationStyle : "unspecified";
      return new ClipboardItem(values, { presentationStyle });
    });
    await navigator.clipboard.write(clipboardItems);
  }

  // plugins/browser-bridge/apps/cli/internal/locator/runtime-src/index.ts
  var VERSION = "5";
  var BUILD_HASH = "99147da636d4f1bb743eb2f18394717d8a42d46f16d9c0d97b4a5e2ef3b49a2f";
  if (window.__actspaceLocator?.version !== VERSION || window.__actspaceLocator.buildHash !== BUILD_HASH) {
    window.__actspaceLocator = createRuntime(VERSION, BUILD_HASH);
  }
})();
